import type { FastifyInstance } from 'fastify';
import type { OperatorAuthHook } from './auth';

export async function registerReviewActionsRoute(_: FastifyInstance, __: OperatorAuthHook) {
  // Placeholder: actual review routes will be registered in Phase 2.
  // Auth hook will be applied when those routes are defined.
}
