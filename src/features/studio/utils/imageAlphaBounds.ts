// Moved to the design system — both app surfaces and the placement editor use it.
// Re-exported here so existing imports keep working.
export {
  FULL_BOUNDS,
  probeAlphaBounds,
  probeGarmentBounds,
  type AlphaBounds,
  type GarmentBounds,
} from "@/design-system/utils/image-alpha-bounds"
